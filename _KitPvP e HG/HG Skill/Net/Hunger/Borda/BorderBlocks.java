package Net.Hunger.Borda;

import java.util.Random;

import org.bukkit.Bukkit;
import org.bukkit.Location;
import org.bukkit.Material;
import org.bukkit.entity.Player;
import org.bukkit.event.EventHandler;
import org.bukkit.event.player.PlayerMoveEvent;

public class BorderBlocks {
		
	public static void GerarBorda() {
	for (int x = -500; x <= 500; x++) {
	if (x == -500 || x == 500) {
	for (int z = -500; z <= 500; z++) {
	for (int y = 0; y <= 130; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -500; z <= 500; z++) {
	if (z == -500 || z == 500) {
	for (int x = -500; x <= 500; x++) {
	for (int y = 0; y <= 130; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -500; x <= 500; x++) {
	if (x == -500 || x == 500) {
	for (int z = -500; z <= 500; z++) {
	for (int y = 0; y <= 500; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -500; z <= 500; z++) {
	if (z == -500 || z == 500) {
	for (int x = -500; x <= 500; x++) {
	for (int y = 0; y <= 500; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -501; x <= 501; x++) {
	if (x == -501 || x == 501) {
	for (int z = -501; z <= 501; z++) {
	for (int y = 0; y <= 131; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -501; z <= 501; z++) {
	if (z == -501 || z == 501) {
	for (int x = -501; x <= 501; x++) {
	for (int y = 0; y <= 131; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -502; x <= 502; x++) {
	if (x == -502 || x == 502) {
	for (int z = -502; z <= 502; z++) {
	for (int y = 0; y <= 132; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -502; z <= 502; z++) {
	if (z == -502 || z == 502) {
	for (int x = -502; x <= 502; x++) {
	for (int y = 0; y <= 132; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -503; x <= 503; x++) {
	if (x == -503 || x == 503) {
	for (int z = -503; z <= 503; z++) {
	for (int y = 0; y <= 133; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -503; z <= 503; z++) {
	if (z == -503 || z == 503) {
	for (int x = -503; x <= 503; x++) {
	for (int y = 0; y <= 133; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -504; x <= 504; x++) {
	if (x == -504 || x == 504) {
	for (int z = -504; z <= 504; z++) {
	for (int y = 0; y <= 134; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -504; z <= 504; z++) {
	if (z == -504 || z == 504) {
	for (int x = -504; x <= 504; x++) {
	for (int y = 0; y <= 134; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -505; x <= 505; x++) {
	if (x == -505 || x == 505) {
	for (int z = -505; z <= 505; z++) {
	for (int y = 0; y <= 135; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -505; z <= 505; z++) {
	if (z == -505 || z == 505) {
	for (int x = -505; x <= 505; x++) {
	for (int y = 0; y <= 135; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -506; x <= 506; x++) {
	if (x == -506 || x == 506) {
	for (int z = -506; z <= 506; z++) {
	for (int y = 0; y <= 136; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -506; z <= 506; z++) {
	if (z == -506 || z == 506) {
	for (int x = -506; x <= 506; x++) {
	for (int y = 0; y <= 136; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -507; x <= 507; x++) {
	if (x == -507 || x == 507) {
	for (int z = -507; z <= 507; z++) {
	for (int y = 0; y <= 137; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -507; z <= 507; z++) {
	if (z == -507 || z == 507) {
	for (int x = -507; x <= 507; x++) {
	for (int y = 0; y <= 137; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -508; x <= 508; x++) {
	if (x == -508 || x == 508) {
	for (int z = -508; z <= 508; z++) {
	for (int y = 0; y <= 138; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -508; z <= 508; z++) {
	if (z == -508 || z == 508) {
	for (int x = -508; x <= 508; x++) {
	for (int y = 0; y <= 138; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -509; x <= 509; x++) {
	if (x == -509 || x == 509) {
	for (int z = -509; z <= 509; z++) {
	for (int y = 0; y <= 139; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -509; z <= 509; z++) {
	if (z == -509 || z == 509) {
	for (int x = -509; x <= 509; x++) {
	for (int y = 0; y <= 139; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	for (int x = -510; x <= 510; x++) {
	if (x == -510 || x == 510) {
	for (int z = -510; z <= 510; z++) {
	for (int y = 0; y <= 140; y++) { 
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded())
	loc.getChunk().load();
	if(new Random().nextBoolean()){
	loc.getBlock().setType(Material.getMaterial(7));
	} else {
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	} for (int z = -510; z <= 510; z++) {
	if (z == -510 || z == 510) {
	for (int x = -510; x <= 510; x++) {
	for (int y = 0; y <= 140; y++) {
	Location loc = new Location(Bukkit.getWorld("world"), x, y, z);
	if (!loc.getChunk().isLoaded());
	loc.getChunk().load();
    if(new Random().nextBoolean()){
    loc.getBlock().setType(Material.getMaterial(7));
	} else { 
	loc.getBlock().setType(Material.getMaterial(7));
	}
	}
	}
	}
	}
	}
}